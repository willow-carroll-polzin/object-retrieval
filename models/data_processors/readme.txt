V1: These process a subset of the MIT dataset and take object labels and x,y positions.
    Both the PKL and CSV versions are functional.

V2: These process a subset of the cleaned MIT dataset and take object labels and x,y positions.
    Only the PKL verion is functional.

V3: These process a subset of the cleaned MIT dataset and take object labels only. 
    Only the PKL verion is functional.